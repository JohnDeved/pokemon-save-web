# Docker Configuration
# Set this to use a specific mGBA image instead of building from source

# Use pre-built image from GitHub Container Registry (recommended)
MGBA_IMAGE=ghcr.io/johndeved/pokemon-save-web/mgba-test-env:latest

# Or use a specific tag
# MGBA_IMAGE=ghcr.io/johndeved/pokemon-save-web/mgba-test-env:main

# Or leave unset to build from source (slower)
# MGBA_IMAGE=