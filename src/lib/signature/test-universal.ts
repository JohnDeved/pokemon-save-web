#!/usr/bin/env tsx
/**
 * DEPRECATED: Simple CLI tool to test universal patterns for finding partyData addresses
 * 
 * This file uses the old universal patterns approach and is deprecated.
 * Use behavioral-scanner.ts for the new behavioral pattern approach.
 */

console.log("⚠️  This tool uses deprecated patterns. Use 'npm run behavioral-cli' instead.");
console.log("   The new behavioral approach finds addresses dynamically without knowing them beforehand.");
process.exit(1);