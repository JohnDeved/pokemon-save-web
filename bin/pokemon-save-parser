#!/usr/bin/env -S deno run --unstable-sloppy-imports --allow-net --allow-read --allow-write --allow-env

// Pokemon Save Parser CLI
// This is a direct import of the main CLI module
export * from '../src/lib/parser/cli.ts'